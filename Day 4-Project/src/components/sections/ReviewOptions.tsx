// src/components/ReviewOptions.tsx
import React from "react";

const ReviewOptions: React.FC = () => {
  return (
    <div className="mt-4">
      {/* Removed predefined review options */}
      {/* Now, we rely on the rating system only for feedback */}
    </div>
  );
};

export default ReviewOptions;
